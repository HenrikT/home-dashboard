{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: dev (strict)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev:strict",
      "cwd": "${workspaceFolder}"
    }
  ]
}
