/// <reference types="vitest" />

// This file ensures the editor and TypeScript server pick up Vitest types
// so test globals like `describe`, `it`, `expect`, and `vi` are recognised.

// No runtime code — types only.
